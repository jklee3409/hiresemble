hiresemble:
  idempotency:
    hmac-keys:
      1: hiresemble-local-only-idempotency-key-change-in-production
  ai:
    provider: ${AI_PROVIDER:openai}
  search:
    provider: ${WEB_SEARCH_PROVIDER:tavily}
spring:
  ai:
    model:
      chat: ${AI_CHAT_MODEL_PROVIDER:openai}
      embedding: ${AI_EMBEDDING_MODEL_PROVIDER:openai}
    vectorstore:
      type: none
